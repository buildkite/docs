{
  "steps": {
    "name": "Steps",
    "textContent": <%= render_attribute_content "steps" %>,
    "attributes": <%= JSON.pretty_generate(@steps_content).indent(4).lstrip.html_safe %>
  },
  "notify": {
    "name": "Notifications",
    "textContent": <%= render_attribute_content "notifications" %>,
    "attributes": <%= JSON.pretty_generate(@notifications_content).indent(4).lstrip.html_safe %>
  }
}
