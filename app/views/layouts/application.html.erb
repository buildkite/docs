<!DOCTYPE html>
<html lang='en'>
  <head>
    <%= render "head" %>
  </head>
  <body <%= beta? ? 'class=beta' : ''-%>>
    <%= render "site_header", nav: default_nav %>

    <main id="main" role="main">
      <div class="PageContainer StandardWhiteContentPage">
        <div class="Page">
          <div class="Page__sidebar">
            <%= render 'sidebar', nav: default_nav %>
          </div>

          <div class="Page__toc">
            <%= yield :toc %>
          </div>

          <div class="Page__content">
            <div class="TextContent">
              <%= yield %>
            </div>
            <%= render "footer", :embed_emojicom => true %>
          </div>
        </div>
      </div>
    </main>
    <%= render "algolia" %>
  </body>
</html>
