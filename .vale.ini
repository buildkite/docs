StylesPath = vale

Vocab = Buildkite

MinAlertLevel = warning

[formats]
erb = md

[*.md]

# Ignore
# {:toc}        - literal toc
# :[a-z]*:      - all emoji words
# <%=.*%>       - ERB spans
# {:[a-z -=]*}  - Codeblock filenames

TokenIgnores = ({:toc}), (:[a-z]*:), (<%=.*%>), ({:[a-z -=]*})

Vale.Terms = NO

BasedOnStyles = Vale, Buildkite

