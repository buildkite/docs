# buildkite-agent workdir

The Buildkite agent's `workdir` subcommands provide the ability to interact with the working directory of the currently running job.

## Set

<%= render 'agent/cli/help/workdir_set' %>

