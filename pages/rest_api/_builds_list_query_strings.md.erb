<table>
<tbody>
  <tr><th><code>creator</code></th><td>Filters the results by the user who created the build<p class="Docs__api-param-eg"><em>Example:</em> <code>?creator=5acb99cf-d349-4189-b361-d1b9f36d70d7</code></p></td></tr>
  <tr><th><code>created_from</code></th><td>Filters the results by builds created on or after the given time (in ISO 8601 format)<p class="Docs__api-param-eg"><em>Example:</em> <code>?created_from=<%= 5.days.ago.utc.iso8601 %></code></p></td></tr>
  <tr><th><code>created_to</code></th><td>Filters the results by builds created before the given time (in ISO 8601 format)<p class="Docs__api-param-eg"><em>Example:</em> <code>?created_to=<%= 1.month.from_now.utc.iso8601 %></code></p></td></tr>
  <tr><th><code>finished_from</code></th><td>Filters the results by builds finished on or after the given time (in ISO 8601 format)<p class="Docs__api-param-eg"><em>Example:</em> <code>?finished_from=<%= 2.days.ago.utc.iso8601 %></code></p></td></tr>
  <tr>
    <th><code>state</code></th>
    <td>
    <span>Filters the results by the given build state. The <code>finished</code> state is a shortcut to automatically search for builds with  <%= API::Resources::BuildsList::FINISHED_STATES.map { |x| "<code>#{x}</code>" }.join(", ").html_safe %> states.</span>
      <p class="Docs__api-param-eg">
        <em>Valid states:</em> <%= API::Resources::BuildsList::SEARCHABLE_STATES.map { |x| "<code>#{x}</code>" }.join(", ").html_safe %>
      </p>
      <p class="Docs__api-param-eg"><em>Example:</em> <code>?state=passed</code> returns all <code>passed</code> builds</p>
      <p class="Docs__api-param-eg"><em>Example:</em> <code>?state[]=scheduled&state[]=running</code> returns all <code>scheduled</code> and <code>running</code> builds</p>
    </td>
  </tr>
  <tr><th><code>meta_data</code></th><td>Filters the results by the given meta_data.<p class="Docs__api-param-eg"><em>Example:</em> <code>?meta_data[some-key]=some-value</code></p></td></tr>
  <tr>
    <th>
      <code>branch</code>
    </th>
    <td>
      <span>Filters the results by the given branch or branches.</span>
      <p class="Docs__api-param-eg"><em>Example:</em> <code>?branch=master</code> returns all builds on the <code>master</code> branch</p>
      <p class="Docs__api-param-eg"><em>Example:</em> <code>?branch[]=master&branch[]=testing</code> returns all builds on <code>master</code> and <code>testing</code> branches</p>
    </td>
  </tr>
    <tr><th><code>commit</code></th><td>Filters the results by the commit (only works for full sha, not for shortened ones).<p class="Docs__api-param-eg"><em>Example:</em> <code>?commit=long-hash</code></p></td></tr>
</tbody>
</table>
