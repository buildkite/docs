{
  "name": "docs",
  "private": true,
  "scripts": {
    "lint": "npx alex@9 -q --diff \"pages/**/*.erb\" \"pages/**/*.txt\"",
    "validate-yaml": "npx --package yaml-schema-validator@1 schema validate -f \"data/agent_attributes.yaml\" -s \"data/agent_attributes.schema.yaml\""
  }
}
