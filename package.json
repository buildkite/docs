{
  "name": "docs",
  "private": true,
  "dependencies": {
    "alex": "^9.0.1",
    "yaml-schema-validator": "^1.2.2"
  },
  "devDependencies": {},
  "scripts": {
    "lint": "alex -q --diff \"pages/**/*.erb\" \"pages/**/*.txt\"",
    "validate-yaml": "schema validate -f \"data/agent_attributes.yaml\" -s \"data/agent_attributes.schema.yaml\""
  }
}
