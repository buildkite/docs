---
type: object
properties:
  variables:
    type: array
    items:
      type: object
      properties:
        name:
          type: string
        default_value:
          type: string
        desc:
          type: string
      required:
        - name
        - desc
required:
  - variables
