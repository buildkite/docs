fields:
- name: method
  required: false
  type: string
  enumerated_values:
    - GET
    - POST
    - PUT
    - DELETE
    - PATCH
    - HEAD
    - CONNECT
    - OPTIONS
    - TRACE
  desc: |
      A HTTP request method (required if parent span has a `section` of `http`)
  examples:
- name: url
  required: false
  type: string
  desc: |
      The URL requested (required if parent span has a `section` of `http`)
  examples:
- name: lib
  required: false
  type: string
  desc: |
     The library used to make the HTTP request (required if parent span has a `section` of `http`)
  examples:
- name: query
  required: false
  type: string
  desc: |
     The SQL query (required if parent span has a `section` of `sql`)
  examples:
- name: content
  required: false
  type: string
  desc: |
    The annotation content (required if parent span has a `section` of `annotation`)
